name: Test Schedule

on:
  schedule:
    - cron: "*/5 * * * *"  # كل 5 دقائق
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "✅ Scheduled workflow is working at $(date)"
